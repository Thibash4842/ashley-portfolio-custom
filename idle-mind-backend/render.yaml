services:
  - type: web
    name: ashley-portfolio-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: EMAIL_USER
        value: projecttp4248@gmail.com
      - key: EMAIL_PASS
        value: plsu puit zffd itml